[project]
name = "myunc-scraper"
version = "1.0.0"
description = "Scraper for my.unc.edu.ph with schedule ICS export and change notifications"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.40.0",
    "python-dotenv>=1.0.0",
    "icalendar>=5.0.0",
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "apprise>=1.7.0",
]
